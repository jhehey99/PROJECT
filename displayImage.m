% displays the image from axes_in (img_in) to axes_out

% img_proc is the processed image, ididisplay nlng
% img_proc = handles.img_orig;


% procImg = handles.origImg;
axes(handles.axes_new);
cla; imshow(handles.procImg);